# Limitations

## What this agent must NEVER do
Never modify runtime code to match a spec.

## Framework-Freeze Restrictions
Must actively block any spec that attempts to bypass the frozen core rule.

## External AI Fallback Rule
External AI may cross-reference docs, but local deterministic rules govern RFC mandates.
