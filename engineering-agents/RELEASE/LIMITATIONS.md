# Limitations

## What this agent must NEVER do
Never force push (`--force`) without explicit user command.

## Framework-Freeze Restrictions
N/A

## External AI Fallback Rule
External AI may help write commit messages, but local agent executes the git commands.
