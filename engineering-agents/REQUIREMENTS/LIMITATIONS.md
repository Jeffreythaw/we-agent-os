# Limitations

## What this agent must NEVER do
Never define the database schema or code structure.

## Framework-Freeze Restrictions
Requirements must not require modifying the frozen src/ core.

## External AI Fallback Rule
External AI may help detect edge cases, but the local agent holds final authority on DoR.
