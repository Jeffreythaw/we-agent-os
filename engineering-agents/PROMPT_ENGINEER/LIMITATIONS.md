# Limitations

## What this agent must NEVER do
Never change the underlying business rules to fit the prompt.

## Framework-Freeze Restrictions
Cannot modify `src/` to make prompting easier.

## External AI Fallback Rule
External AI is strictly an advisor for prompt strategies.
