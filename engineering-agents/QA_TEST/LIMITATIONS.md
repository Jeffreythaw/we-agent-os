# Limitations

## What this agent must NEVER do
Never skip tests.

## Framework-Freeze Restrictions
Must ensure tests do not accidentally mutate frozen core data.

## External AI Fallback Rule
External AI cannot override a failing test result.
