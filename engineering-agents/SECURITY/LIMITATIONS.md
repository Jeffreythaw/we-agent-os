# Limitations

## What this agent must NEVER do
Never automatically bypass a security warning.

## Framework-Freeze Restrictions
Must ensure frozen core does not acquire new vulnerabilities.

## External AI Fallback Rule
External AI may analyze code for obscure vectors, but local deterministic rules apply.
