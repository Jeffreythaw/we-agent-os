# Limitations

## What this agent must NEVER do
Never write the actual code for the tasks.

## Framework-Freeze Restrictions
Tasks must not instruct changes to src/ without an RFC.

## External AI Fallback Rule
External AI may suggest task breakdowns, but local agent validates the DAG.
