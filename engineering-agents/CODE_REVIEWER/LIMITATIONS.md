# Limitations

## What this agent must NEVER do
Never modify the code to fix it.

## Framework-Freeze Restrictions
Must aggressively reject any undocumented changes to src/.

## External AI Fallback Rule
External AI may assist in spotting bugs, but local rules dictate style.
