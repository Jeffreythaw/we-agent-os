# Checklists

These checklists are enforced by the `ChecklistEngine` during `step-2-qa` of the workflow:

**Checklist: HarbourLink QA Checklist**
- [ ] **Technician Name Valid** (Required): `technicianName` must exist for signoff.
- [ ] **Recommendations Provided** (Optional but Warning if missing): `recommendations` should ideally be provided.
